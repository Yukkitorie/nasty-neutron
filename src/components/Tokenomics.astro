---
import { Image } from "astro:assets";
import pieChart from '../assets/icons/pieChart8.svg';
import circle_3 from '../assets/icons/circle_3.svg';
const tokenomics = [
{ name: 'Free Float:', value: '50.0%' },
{ name: 'Liquidity:', value: '12.2%' },
{ name: 'Founder:', value: '7.8%' },
{ name: 'Pre-sale:', value: '10.0%' },
{ name: 'Staking:', value: '10.0%' },
{ name: 'Developers:', value: '5.0%' },
{ name: 'Marketing:', value: '5.0%' }
];
---

<section id="tokenomics"></section>
<h1>Tokenomics</h1>
<div class="container">
<div class="card">
<div class="wrapper">
    <div class="pie_diagram">
        <Image src={pieChart} alt="" class="pie-img"/>
    </div>
    <div class="token-list">
        {tokenomics.map(token => (
            <div class="token-item">
                <div class="token-info">
                    <Image src={circle_3} alt="" class="img"/>
                    <span class="token-name">{token.name} </span>
                    <span class="token-value">{token.value}</span>
                </div>
            </div>
        ))}
    </div>
</div>
</div>
</div>


<style scoped>
    .container{
    }
    .card{
        width: 100%;
        background-color: #131B26;
        border-radius: 1rem; 
        padding: 0rem; 
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.2); 
        max-width: 1200px;
        margin: 2rem auto; 
    }
    .wrapper {
        display: flex;
        align-items: center;
        gap: 2rem;
        
    }

    .pie_diagram {
        flex: 0 0 auto;
        width: 700px;
        height: 650px;
    }

    .pie-img {
        
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    .token-list {
        flex: 0 0 auto;
        padding: 5rem;
    }

    .token-item {
        display: flex;
        align-items: center;
        margin-bottom: 0.75rem;
    }

    .token-item:last-child {
        margin-bottom: 0;
    }

    .token-info {
        display: flex;
        justify-content: space-between;
        align-items: center;
        color: whitesmoke;
        opacity: 0.9;
        gap: 0.5rem;
    }

    .img {
        width: 24px;
        height: auto;
    }

    .token-name, 
    .token-value {
        font-size: 1.5rem;
    }

    @media (max-width: 1399px) {
        .wrapper {
            padding: 0;
            gap: 0;
        }
    }
    @media (max-width: 1060px) {
    .token-name, 
    .token-value {
        font-size: 1.2rem;
    }
        .wrapper {
            flex-direction: column;
            justify-content: center;
            padding: 0;
            gap: 0;
        }

        .pie_diagram {
            width: 370px;
            height: 370px;
        }
        .pie-img {
            margin-top: 1.5rem;
        }
    }
</style>